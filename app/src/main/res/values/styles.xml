<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Fullscreen activity -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>

        <!-- Popup menu background -->
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
        <item name="android:textAppearanceLargePopupMenu">
            @style/AppTheme.TextAppearance.Popup.Large
        </item>
        <item name="android:textAppearanceSmallPopupMenu">
            @style/AppTheme.TextAppearance.Popup.Small
        </item>

        <!-- Colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Text -->
    <style name="GameText">
        <item name="android:fontFamily">@font/orbitron_medium</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="GameText.Bold">
        <item name="android:fontFamily">@font/orbitron_bold</item>
    </style>

    <!-- Button -->
    <style name="GameButton">
        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:fontFamily">@font/orbitron_black</item>
    </style>

    <!-- SeekBar -->
    <style name="GameSeekBar">
        <item name="android:maxHeight">8dp</item>
        <item name="android:progressDrawable">@drawable/seekbar_progress</item>
    </style>

    <!-- Popup Menu -->
    <style name="PopupMenu" parent="@android:style/Widget.PopupMenu">
        <item name="android:popupBackground">@drawable/game_pause_dialog_background</item>
    </style>

    <style name="AppTheme.TextAppearance.Popup.Large" parent="TextAppearance.AppCompat.Light.Widget.PopupMenu.Large">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:fontFamily">@font/orbitron_medium</item>
    </style>

    <style name="AppTheme.TextAppearance.Popup.Small" parent="TextAppearance.AppCompat.Light.Widget.PopupMenu.Small">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:fontFamily">@font/orbitron_medium</item>
    </style>

</resources>
